import 'package:flutter/rendering.dart';

class ProjectPadding extends EdgeInsets {
  const ProjectPadding.all() : super.all(8.0);
  const ProjectPadding.onlyTop() : super.only(top: 8.0);
  const ProjectPadding.onlyRightDouble() : super.only(right: 16.0);
}